var tagElement = document.getElementsByTagName('li');
var classElement = document.getElementsByClassName('content');

for (var i = 0; i < tagElement.length; i++) {
	if (classElement = "content") {
			console.log(tagElement[i].innerHTML);
	};

};

console.log(classElement);
